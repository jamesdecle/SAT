---
description: Show a student's current mastery table and a parent-ready summary
argument-hint: <name>
---

Read CLAUDE.md, then `students/$ARGUMENTS`'s `profile.md`, `scores.md`, and
`log.md`.

Show the current skill mastery table from `scores.md` (recompute first if
anything logged since the table was last built isn't reflected yet). Then
write a three-sentence summary suitable for sending to a parent: where the
student stands, what's improved, and what we're focused on next. Keep it
plain-language, no jargon, no internal file paths.
