---
description: Rebuild or show a student's week-by-week schedule
argument-hint: <name>
---

Read CLAUDE.md, then `students/$ARGUMENTS/profile.md`, `scores.md`, and the
current `schedule.md`.

Rebuild (or if nothing has changed, just show) the week-by-week table in
`students/$ARGUMENTS/schedule.md` from today to the exam date, per the
Schedule rule in CLAUDE.md: full-length Bluebook practice tests every 2-3
weeks, weekly in the last 4 weeks, never in the final 3 days; a review
session immediately after every practice test; every row linked to the
files it uses. Base skill sequencing on the current mastery table in
`scores.md`. Note what changed from the previous version, if anything, and
why.
